# MedScans Frontend

Run the API from the project root:

```powershell
dotnet run --launch-profile http
```

Run the React app from this folder:

```powershell
npm.cmd install
npm.cmd start
```

Open:

```text
http://127.0.0.1:5173
```
